// .debug()
// Usar como chainer para invocar o debugger do navegador, printa os argumentos e o target no console.
// Ex: cy.title().debug()

// cy.pause()
// Pausa a execução no Test Runner do Cypress. Para retornar é preciso clicar no botão de play.